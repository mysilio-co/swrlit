{
  "name": "swrlit",
  "version": "0.0.1",
  "description": "it swirls the web in your cup",
  "main": "./dist/swrlit.min.js",
  "types": "./dist/swrlit.d.ts",
  "scripts": {
    "dev": "npm run tslint && webpack --mode development",
    "dev:watch": "nodemon --exec 'npm run dev || exit 1'",
    "tslint": "tslint --project .",
    "build": "npm run tslint && webpack --mode production",
    "test": "npm run clean && jest && npm run demo:test && npm run clean",
    "test:coverage": "npm run clean && jest --coverage && npm run demo:test && npm run clean",
    "clean": "rm -rf dist",
    "demo:test": "npm run build && cd demo && npm install && npm run add-self && npm test"
  },
  "author": "Travis Vachon <travis@itme.email>",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^23.3.7",
    "@types/node": "^10.12.18",
    "@types/source-map": "0.5.2",
    "@types/webpack": "^4.4.21",
    "jest": "^23.6.0",
    "nodemon": "^1.17.5",
    "ts-jest": "^23.10.4",
    "ts-loader": "^5.2.2",
    "ts-node": "^7.0.1",
    "tslint": "^5.10.0",
    "typescript": "^2.9.2",
    "webpack": "^4.12.0",
    "webpack-cli": "^3.0.8"
  }
}